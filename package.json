{
  "name": "aionv-power-record",
  "version": "0.1.0",
  "description": "AION V Power Record",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "npm run build && clasp push",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0",
    "@types/google-apps-script": "^1.0.83",
    "typescript": "^5.8.0"
  }
}
